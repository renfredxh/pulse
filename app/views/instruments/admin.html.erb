<%= content_for :admin_content do %>
  <h1>Instruments</h1>

  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Category</th>
        <th>ID</th>
        <th>Sample</th>
        <th>Preview</th>
        <th></th>
        <th></th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @instruments.each do |instrument| %>
        <tr>
          <td><%= instrument.name %></td>
          <td><%= instrument.category %></td>
          <td><%= instrument.id %></td>
          <td><%= instrument.sample %></td>
          <td>
            <audio controls="controls">
              <source src="<%= instrument.sample %>.wav" type="audio/wav"/>
            </audio>
          </td>
          <td><%= link_to 'Show', instrument %></td>
          <td><%= link_to 'Edit', edit_instrument_path(instrument) %></td>
          <td><%= link_to 'Destroy', instrument, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Instrument', new_instrument_path %>
<% end %>
<%= render template: "layouts/admin" %>
